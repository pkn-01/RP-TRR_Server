# ระบบแจ้งซ่อม LINE OA Integration Guide

## 📋 ภาพรวม

ระบบแจ้งซ่อมได้รับการอัพเดตเพื่อรองรับการแจ้งซ่อมผ่าน LINE Official Account (LINE OA) โดยผู้ใช้สามารถ:

1. **สร้างแจ้งซ่อม** โดยไม่ต้องเชื่อมต่อบัญชีหรือเข้าสู่ระบบ
2. **กรอกข้อมูลเพิ่มเติม** เช่น เบอร์โทรศัพท์ และ LINE ID (ไม่บังคับ)
3. **ติดตามสถานะ** ของการแจ้งซ่อมผ่าน LINE Chat

---

## 🚀 ฟีเจอร์หลัก

### 1. Rich Menu ใน LINE OA

เมื่อผู้ใช้พูดคุยกับ LINE OA ของคุณ ระบบจะตอบกลับด้วย Flex Message ที่มี 3 ปุ่ม:

```
📋 สร้างแจ้งซ่อมใหม่  → ไปยังฟอร์มสร้าง Ticket
📊 ดูสถานะการแจ้งซ่อม  → ดูความคืบหน้า
🔗 เชื่อมต่อบัญชี      → เชื่อมต่อกับบัญชีในระบบ
```

### 2. ฟอร์มแจ้งซ่อมแบบ Lightweight

ฟอร์มสำหรับ LINE OA ได้รับการออกแบบให้:
- **เรียบง่าย** - เน้นข้อมูลที่จำเป็น
- **ไม่บังคับ** - อนุญาตให้ผู้ใช้ข้ามข้อมูลทั่วไป (เบอร์โทร, LINE ID)
- **Mobile-Friendly** - ปรับแต่งสำหรับมือถือ

#### ขั้นตอนของฟอร์ม:
1. **ประเภทปัญหา** - เลือกประเภทและประเภทย่อย (บังคับ)
2. **ข้อมูลอุปกรณ์** - ชื่ออุปกรณ์และสถานที่
3. **รายละเอียด** - หัวเรื่องและรายละเอียด
4. **ข้อมูลติดต่อ** - เบอร์โทรและ LINE ID (ไม่บังคับ)

### 3. ดูสถานะผ่าน LINE

ผู้ใช้สามารถดูสถานะการแจ้งซ่อมและประวัติการอัพเดตผ่านลิงก์ที่ LINE ส่งมา

---

## 🛠️ เส้นทาง API

### 1. สร้างแจ้งซ่อม (ผ่าน LINE)

```
POST /api/tickets/line-oa
Headers: X-LINE-USER-ID: {lineUserId}
Content-Type: application/x-www-form-urlencoded or multipart/form-data

Body:
{
  "title": "หัวเรื่อง",
  "description": "รายละเอียด",
  "problemCategory": "HARDWARE",
  "problemSubcategory": "MONITOR_BROKEN",
  "equipmentName": "Monitor ห้อง 101",
  "location": "ห้อง 101",
  "priority": "MEDIUM",
  "phoneNumber": "08X-XXXX-XXXX",      // optional
  "lineId": "line-id",                 // optional
  "files": [...]                       // optional
}

Response:
{
  "id": 1,
  "ticketCode": "TKT-2025-123456",
  "title": "หัวเรื่อง",
  "status": "OPEN",
  "createdAt": "2025-01-23T10:00:00Z"
}
```

### 2. ดึงข้อมูล Ticket (รวมสถานะ)

```
GET /api/tickets/{id}

Response:
{
  "id": 1,
  "ticketCode": "TKT-2025-123456",
  "title": "...",
  "description": "...",
  "status": "OPEN|IN_PROGRESS|DONE",
  "priority": "LOW|MEDIUM|HIGH",
  "equipmentName": "...",
  "location": "...",
  "user": { "id": 1, "name": "..." },
  "assignee": { "id": 2, "name": "..." },
  "logs": [
    {
      "id": 1,
      "status": "OPEN",
      "comment": "...",
      "user": { "name": "IT Staff" },
      "createdAt": "..."
    }
  ],
  "createdAt": "...",
  "updatedAt": "..."
}
```

### 3. ส่งสถานะไปยัง LINE

```
POST /api/line-oa/send-ticket-status

Body:
{
  "lineUserId": "{lineUserId}",
  "ticketId": 1,
  "ticketCode": "TKT-2025-123456",
  "status": "IN_PROGRESS",
  "statusLabel": "กำลังซ่อม"
}

Response:
{
  "success": true,
  "message": "Ticket status sent to LINE"
}
```

---

## 🔄 Flow การทำงาน

### เมื่อผู้ใช้สร้างแจ้งซ่อมผ่าน LINE OA:

```
1. ผู้ใช้พูดคุยกับ LINE OA
   ↓
2. LINE Webhook เรียก handleMessage()
   ↓
3. ส่ง Welcome Menu กลับไปยัง LINE
   ↓
4. ผู้ใช้คลิก "สร้างแจ้งซ่อมใหม่"
   ↓
5. ไปยังหน้า /tickets/create-line-oa?lineUserId={lineUserId}
   ↓
6. ผู้ใช้กรอกฟอร์มและส่ง
   ↓
7. POST /api/tickets/line-oa
   ↓
8. สร้าง Ticket ในระบบ
   ↓
9. ตอบกลับด้วย ticketCode และ ticketId
   ↓
10. ผู้ใช้เห็นข้อความสำเร็จ
```

### เมื่อ IT ทีมอัพเดตสถานะ:

```
1. IT Staff อัพเดตสถานะใน Dashboard
   ↓
2. POST /api/line-oa/send-ticket-status
   ↓
3. LINE Service ส่ง Flex Message ไปยัง LINE User
   ↓
4. ผู้ใช้ได้รับการแจ้งเตือน
```

---

## 📁 ไฟล์ที่เพิ่มและแก้ไข

### เพิ่มใหม่:
- `/frontend/src/app/tickets/create-line-oa/page.tsx` - ฟอร์มสร้าง Ticket สำหรับ LINE
- `/frontend/src/app/tickets/line-oa-status/page.tsx` - หน้าดูสถานะสำหรับ LINE

### แก้ไข:
- `/backend/src/line-oa/line-oa-webhook.service.ts` - เพิ่ม Rich Menu และฟังก์ชั่นส่งข้อมูล
- `/backend/src/line-oa/line-oa.controller.ts` - เพิ่ม endpoint สำหรับส่งสถานะ
- `/backend/src/tickets/tickets.controller.ts` - เพิ่ม endpoint `/api/tickets/line-oa`
- `/backend/src/tickets/tickets.service.ts` - เพิ่ม method `createLineOATicket()`
- `/backend/src/tickets/dto/create-ticket.dto.ts` - เพิ่ม fields สำหรับ LINE OA

---

## 🔐 การจัดการข้อมูลผู้ใช้

### สำหรับผู้ใช้ที่ยังไม่ได้เชื่อมต่อบัญชี:

1. ระบบจะสร้าง User ชั่วคราวตามรูปแบบ:
   - Name: `LINE User {last-8-chars-of-lineUserId}`
   - Email: `line-{last-8-chars-of-lineUserId}@lineoa.local`
   - Role: `USER`

2. ข้อมูลติดต่อ (เบอร์โทร, LINE ID) จะเก็บใน `notes` field

### สำหรับผู้ใช้ที่เชื่อมต่อบัญชีแล้ว:

- จะใช้ User ID จากการเชื่อมต่อ
- ทุกข้อมูลจะเก็บในบัญชีหลัก

---

## 🎨 UI Components ที่ใช้

### Frontend:
- `InputField` - สำหรับข้อมูล Text
- `SelectField` - สำหรับเลือกประเภท
- `FileUpload` - สำหรับอัพโหลดไฟล์
- Responsive Design สำหรับ Mobile

### Backend LINE Integration:
- Flex Message - Rich Message Format
- Postback Data - สำหรับปุ่มโต้ตอบ
- Rich Menu - เมนูประจำตัวของ OA

---

## 🧪 การทดสอบ

### ทดสอบฟอร์ม:

```bash
# เปิดฟอร์มด้วยพารามิเตอร์ LINE
http://localhost:3000/tickets/create-line-oa?lineUserId=U1234567890abcdef1234567890abcd

# ส่งฟอร์ม
POST http://localhost:3000/api/tickets/line-oa
Headers: X-LINE-USER-ID: U1234567890abcdef1234567890abcd
```

### ทดสอบการส่งสถานะ:

```bash
POST http://localhost:3001/api/line-oa/send-ticket-status
Body:
{
  "lineUserId": "U1234567890abcdef1234567890abcd",
  "ticketId": 1,
  "ticketCode": "TKT-2025-123456",
  "status": "IN_PROGRESS",
  "statusLabel": "กำลังซ่อม"
}
```

---

## 📱 Mobile Responsiveness

ฟอร์ม LINE OA ได้รับการออกแบบให้เต็มที่บนอุปกรณ์มือถือ:

- ปรับขนาดอัตโนมัติ
- ปุ่มขนาดใหญ่ (44px+)
- ช่องป้อนข้อมูลมีระยะห่างที่เพียงพอ
- ไม่ต้องซูมเพื่อกรอกข้อมูล

---

## 🚨 Error Handling

### จะดำเนินการกับ Error ต่อไปนี้:

1. **Missing Required Fields** - ส่งกลับ 400 Bad Request
2. **Invalid File Upload** - จำกัดขนาด 5 ไฟล์ @ 10MB ต่อไฟล์
3. **LINE Connection Error** - Log ข้อมูล แต่สร้าง Ticket ได้เสมอ
4. **Database Error** - ส่งกลับ 500 Internal Server Error

---

## 🔗 Environment Variables

เพิ่มในไฟล์ `.env`:

```env
# LINE OA Configuration
LINE_CHANNEL_SECRET=your_channel_secret
LINE_ACCESS_TOKEN=your_access_token
LINE_OA_ID=your_oa_id

# Frontend URL
NEXT_PUBLIC_FRONTEND_URL=http://localhost:3000
```

---

## 📞 Support & Troubleshooting

### Webhook ไม่ได้รับข้อมูล:
- ตรวจสอบ LINE_CHANNEL_SECRET
- ตรวจสอบ URL ของ Webhook
- ดูใน LINE Console เพื่อยืนยันการส่ง

### Ticket ไม่ถูกสร้าง:
- ตรวจสอบ Logs ในเซิร์ฟเวอร์
- ดู Database เพื่อยืนยันข้อมูล
- ตรวจสอบ Validation errors

### ผู้ใช้ไม่ได้รับข้อความ:
- ตรวจสอบว่า LINE User ID ถูกต้อง
- ทดสอบด้วย Manual API call
- ตรวจสอบสิทธิ์การส่งข้อมูล
